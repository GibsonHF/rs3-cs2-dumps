//
function script8739(): void {
    if ((script6431() == true)) {
        IF_SETTEXT("Instructions (Tap to close)", comp(1053, 42));
        IF_SETTEXT("Tap on a piece to select it, and again on the grid to place it.<br><br>To Deselect a piece, tap on the Deselect button.<br><br>To return a piece from the grid, tap and hold on it and select 'Reset'.<br><br>The 'RESET' button clears the grid.", comp(1053, 43));
    };
    return;
}