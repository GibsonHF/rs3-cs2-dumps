//
function script19377(int0: struct): int {
    switch (int0) {
        default: {
        }
    };
    script12478(`GET_CLAN: The struct for '${struct_getparam(int0, 7527)}' does not have an entry but is flagged to use the clan system.`);
    return 0;
}