//[clientscript,scrollbar_vertical_jump]
function script34(int0: number, int1: number, int2: number): void {
    if ((CC_FIND(int0, 1) == 1)) {
        CC_DRAGPICKUP(0, (CC_GETHEIGHT() / 2));
    };
    return;
}