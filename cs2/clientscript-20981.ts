//
function script20981(): void {
    switch (2) {
        case 1: {
            stack(dbrow_getfield(int0, 1368080, 0));
            return;
        }
        case 2: {
            stack(dbrow_getfield(int0, 1368096, 0));
            return;
        }
    };
    script12478(`League ${inttostring(2, 10)} has not been plugged in to task validation.`);
    stack(0);
    return;
}