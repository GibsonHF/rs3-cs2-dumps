//
function script18128(int0: component): void {
    if ((script6431() == true)) {
        IF_SETTEXT("You can open the Hero Pass via the main screen Hero Pass button.", int0);
    } else {
        IF_SETTEXT("You can open the Hero Pass via the Upgrades and Extras interface.", int0);
    };
    return;
}