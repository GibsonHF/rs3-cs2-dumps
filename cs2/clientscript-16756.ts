//
function script16756(string0: string): string {
    var string0 = `${string0}- Attacks have a <col=ffffff>${inttostring(25, 10)}%</col> chance to reduce the target's stats by <col=ffffff>${inttostring(1, 10)}</col>.`;
    return string0;
}