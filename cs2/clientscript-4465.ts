//[proc,clan_chat_kick_find]
function script4465(string0: string): void {
    if ((unk10993(0) == 1)) {
        script4466(string0);
    } else {
        printmessage("You must be in your clan channel to do that.");
    };
    return;
}