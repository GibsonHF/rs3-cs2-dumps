//
function script20955(): void {
    if (((MAP_MEMBERS() == 0) && (dbrow_getfield(int0, 1388640, 0) == 0))) {
        var string0 = strconcat(string0, "<br>- You must be on a members world to teleport here.");
        var int1 = 0;
    };
    stack(int1);
    stack(string0);
    return;
}