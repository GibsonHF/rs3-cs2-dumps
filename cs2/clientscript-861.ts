//
function script861(): void {
    var int0 = (IF_GETWIDTH(comp(682, 1)) - 2);  // catcon_calibration_puzzle_control:power_bar
    IF_SETSIZE(SCALE(int0, 6, varbitplayer_12214), 2, 0, 1, comp(682, 11));  // catcon_calibration_puzzle_control:power
    return;
}