//
function script8796(int0: number, int1: number): void {
    var string0 = "View all items.<br>Right-click to toggle tabs between the top and side.";
    if ((script6431() == 1)) {
        string0 = "View all items.<br>Hold to toggle tabs between the top and side";
    };
    script8800(string0, int0, int1);
    return;
}