//
function script4441(int0: unknown_int, int1: component): void {
    script4445(int1);
    var string0 = "";
    var string1 = "";
    if (((int0 > 0) && (int0 < 8))) {
        return;
    };
    string0 = strconcat(string0, "To join a clan channel as a<br>guest, use the button in the top-left<br>and enter the name of<br>the clan you wish to chat in.<br><br>To talk, start your chat with ///.<br>If you belong to another clan, you<br>can still talk to them by<br>starting your chat with //.");
    return;
}